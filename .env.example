# Supabase project (xtrapxzbfuovnutldete) - frontend credentials.
# The anon key is safe to expose to the browser; RLS restricts every table
# to authenticated admins (private.is_admin()).
VITE_SUPABASE_URL=https://xtrapxzbfuovnutldete.supabase.co
VITE_SUPABASE_ANON_KEY=
